---
name: UiModalLoading
route: '/ui-modal-loading'
---

import { Props, Playground } from 'docz'
import IFramePlayground from 'docz-iframe-playground'
import { UiModalLoading } from './ui-modal-loading'

# UiModalLoading

UiModalLoading shows a full-screen modal overlay with a spinner. Pass in the **isOpen** prop to control visibility on the screen.

## Example

<Playground>
  <IFramePlayground minHeight={400} maxHeight={800}>
    <UiModalLoading isOpen={true} />
  </IFramePlayground>
</Playground>

## Props

<Props of={UiModalLoading} />
